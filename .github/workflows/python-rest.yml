name: tests
on:  push
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.7", "3.8", "3.9", "3.10", "3.11"]
    steps:
      - uses: actions/setup-python@v3
      - name: All tests
        run: bash -xv ./tests/test_computer.bash

